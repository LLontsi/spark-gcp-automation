global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets:
{% for host in groups['all'] %}
        - '{{ hostvars[host]["ansible_host"] | default(hostvars[host]["ansible_ssh_host"] | default(host)) }}:9100'
{% endfor %}
